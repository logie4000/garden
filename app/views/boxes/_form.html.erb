<%= form_with(model: box, local: true) do |form| %>
  <% if box.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(box.errors.count, "error") %> prohibited this box from being saved:</h2>

      <ul>
      <% box.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :label %>
    <%= form.text_field :name, id: :box_label %>
  </div>

  <div class="field">
    Type:
    <%= collection_select(:box, :box_type, ["3x3", "3x5", "4x4"], :to_s, :to_s) %> 
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
