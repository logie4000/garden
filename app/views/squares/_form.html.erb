<%= form_with(model: square, local: true) do |form| %>
  <% if square.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(square.errors.count, "error") %> prohibited this square from being saved:</h2>

      <ul>
      <% square.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :row %>
    <%= @square.row %>
  </div>
  
  <div class="field">
    <%= form.label :column %>
    <%= @square.column %>
  </div>

  <div class="field">
    <%= form.label :has_trellis %>
    <%= form.check_box :square, :has_trellis %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
