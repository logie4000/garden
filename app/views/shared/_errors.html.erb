<% unless (obj.nil?) %>
  <% if obj.errors.any? %>
    <div id="errorExplanation">
      <h2><%= pluralize(obj.errors.count, "error") %> prohibited this record from being saved:</h2>
      <ul>
      <% obj.errors.full_messages.each do |msg| %>
        <% Rails.logger.debug "ERROR reported: #{msg}" %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<% end %>

