<p>
<span class="ad-back-link"><%= link_to 'Back', @user %></span>
<span class="ad-users-link"><%= link_to 'Users', users_path %></span>
</p>

<h1>Editing user</h1>
<%= render :partial => 'shared/errors', :locals => { obj: @user } %>

<%= form_for(@user) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f }  %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

